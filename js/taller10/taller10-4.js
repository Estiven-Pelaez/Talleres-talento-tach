// Declara tres variables llamadas x, y y z y asígnales valores booleanos, utiliza los operadores lógicos &&, || y ! para realizar operaciones lógicas con estas variables, y muestra los resultados de las operaciones lógicas en ventanas de alerta.

let x = true;
let y = false;
let z = true;

alert ('punto 8: Declara tres variables llamadas x, y y z y asígnales valores booleanos, utiliza los operadores lógicos &&, || y ! para realizar operaciones lógicas con estas variables, y muestra los resultados de las operaciones lógicas en ventanas de alerta.')

alert('x=true   y=false  z=true')

alert('x && y: ' + (x && y));
alert('z || y: ' + (z || y));
alert('y && x: ' + (z && y));