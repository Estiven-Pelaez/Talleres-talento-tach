// Declara dos variables llamadas a y b y asígnales valores diferentes, utiliza los operadores de comparación >, <, >=, <=, == y === para comparar a y b, y muestra los resultados de las comparaciones en ventanas de alerta.

let a = 9;
let b = 16;

alert('Punto 7: Declara dos variables llamadas a y b y asígnales valores diferentes, utiliza los operadores de comparación >, <, >=, <=, == y === para comparar a y b, y muestra los resultados de las comparaciones en ventanas de alerta.');
alert('a= 9        b= 16');

alert("a > b: " + (a > b));
alert('a < b: ' + (a < b));
alert('a >= b: ' + (a >= b));
alert('a <= b: ' + (a <= b));
alert('a == b: ' + (a == b));
alert('a === b: ' + (a === b));